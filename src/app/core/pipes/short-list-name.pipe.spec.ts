import { ShortListNamePipe } from './short-list-name.pipe';

describe('ShortListNamePipe', () => {
  it('create an instance', () => {
    const pipe = new ShortListNamePipe();
    expect(pipe).toBeTruthy();
  });
});
